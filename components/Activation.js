const hrStyle = {
  //borderTop: '2px solid #14a3bb',
  marginTop: 50,
  //maxWidth: '50%',
  align: 'right', 
};

const h2Style = {
  textAlign: 'center',
  color: '#000',
};

const h6Style = {
  textAlign: 'center',
  color: '#000',
};

const Container = {
 
  backgroundColor: '#14a3bb',
};


const Activation = props => (
  <div id="activation">
    
    
  </div>
);

export default Activation;
